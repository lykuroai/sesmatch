# ローカルサーバ（lykuro-connector）仕様書 v0.1 ドラフト

作成日: 2026-08-17 ／ 状態: **検討中ドラフト**（確定仕様ではない）

本書は、SESマッチングプラットフォーム（ses.lykuro.ai、以下「親サーバ」）と連携する
**ローカルサーバ**の仕様を定める。プラットフォーム本体の正となる仕様は
`ses_matching_platform_complete_spec_v2_0.md` であり、本書はその追加仕様（連携ツール編）に位置づける。

---

## 1. 目的と位置づけ

- 各テナント企業が**自社環境内**で案件・人材書類を収集・構造化・保管し、
  **商談開始のタイミングで必要な1件だけを親サーバへ送信**するためのツール。
- データ主権は各社に残る。「全データをプラットフォームに預けるのは抵抗がある」という
  導入障壁を解消し、「自社の書類整理ツールとして使い始め、商談に出す分だけ登録する」
  段階的導入を可能にする。
- 公開リポジトリ: `https://github.com/lykuroai/lykuro-connector`（公開時期・ライセンスは未確定）
- 標準品としてのホスティングは行わない。**参考実装の提供まで**とし、構築・運用責任は各社にある。

## 2. 機能仕様

### 2.1 処理フロー

```
書類（フォルダ投入）
  → テキスト抽出（テキスト系形式のみ・OCRなし）
  → PII匿名化（ローカル正規表現処理。本体 pii.ts と同等ロジック）
  → 自前LLMで種別判定・構造化抽出
  → ローカル保存（原本 + 抽出JSON = 自社の案件・人材在庫）

商談開始時: 選択した案件・人材を親サーバへ送信 → 以後は親サーバの既存フロー
（登録 → 公開 → エントリー → 双方承認 → 契約 → 手数料）
```

### 2.2 動作モード

| モード | 動作 |
|---|---|
| ローカル保存（既定） | 親へ送らず、解析結果とともに自社内に保管 |
| 親サーバへ送信 | 商談開始時に、その1件を親サーバへ送信 |

- 送信は書類単位・明示操作で行う（自動一括送信はしない）。
- 送信後のデータの正は親サーバ側とする。ローカル側との同期機能は持たない
  （送信時点のスナップショットを登録する割り切り）。

### 2.3 対応ファイル形式

- テキスト系のみ: .txt / .csv / .md / Word (.docx) / Excel (.xlsx, .xls) / テキスト層のあるPDF
- **OCRは搭載しない**。画像・スキャン書類は親サーバへ直接送信すれば親側で解析される旨を案内する。

### 2.4 種別の扱い

- 案件 / 人材はフォルダ（または画面操作）で指定し、親サーバ送信時は `expectedKind` を付与する。
  親サーバ側の種別チェック（不一致は登録拒否）が最終防壁となる。
- 人材は1書類=1名。案件は親サーバ送信時に親側で複数件自動分割される。

## 3. 自前LLM解析

- 接続先は **OpenAI互換API** とし、各社が自社契約のAPIキーまたは社内LLM（Ollama等）を設定する。
  LLM費用・鍵管理は各社持ち。
- 種別判定・構造化抽出のプロンプトとJSON出力スキーマは参考実装に同梱する
  （※本体の抽出ロジックの一部公開になるため、公開前にkakuの判断を要する）。
- **外部LLMへ送る前に必ずローカル匿名化を通す**構成を標準とし、READMEで作法として明示する。
- ローカル抽出結果は各社の参考データであり、親サーバの正規化（スキル辞書・名寄せ・人手確認）
  とは一致しない場合がある。親へ送信した場合は親側の処理結果を正とする。

## 4. 親サーバ連携

### 4.1 認証 — PAT（APIトークン）方式【親サーバ側の追加開発】

- トークン形式: `ses_pat_` + ランダム32バイトhex。`Authorization: Bearer` で送信。
  DBにはSHA-256ハッシュのみ保存し、平文は発行時に一度だけ表示。
- **メンバー単位で発行**。メンバー退職・無効化、企業停止で自動失効。
- 権限スコープ（メンバー本人の権限との積集合で実効権限を決定）:
  - `ingest`（既定）= `ingestion.create`
  - `ingest-register` = 上記 + `engineer.create` / `project.create`
  - エントリー承認・契約・請求・メンバー管理はスコープに含めない。
- 有効期限: 90日 / 1年（既定） / 無期限。期限30日前に発行者へ通知。
- コンソール「APIトークン」タブ: 発行（名前・スコープ・期限）／一覧（最終使用日時含む）／失効。
- 監査: `ApiTokenIssued` / `ApiTokenRevoked` を記録。PAT経由の操作は発行メンバーの操作として記録。
- PAT実装までの暫定運用: 取込専用の担当者アカウント（最小権限ロール）＋パスワードは
  環境変数または権限600の設定ファイルで保持。

### 4.2 使用API

| API | 用途 |
|---|---|
| `POST /api/v1/ingestions` | 書類のまま送信（親側でPII匿名化→LLM解析→人手確認） |
| `POST /api/v1/projects` / `POST /api/v1/engineers` | ローカル構造化済みデータの直接登録（親のLLM費用ゼロ。画面手入力と同格） |
| `GET /api/v1/ingestions/:id` | 取込状況の確認 |

- 商談開始時の送信にどちらのAPIを使うかは**未確定論点**（§8）。

## 5. ローカルサーバ自体のログイン（社内利用者の管理）

| 段階 | 方式 |
|---|---|
| MVP | 認証なし。localhost または社内LAN限定で運用（外部非公開をREADMEで必須要件化） |
| 拡張 | 親アカウントで認証委譲（ログイン時に親の `/auth/login` で検証。ID二重管理なし、親側の無効化が即反映） |

## 6. インフラ環境（追加仕様）

### 6.1 動作要件（最小）

| 項目 | 要件 |
|---|---|
| 実行環境 | Node.js 20 以上（Windows / Linux / macOS） |
| CPU / メモリ | 1コア / 1〜2GB（OCRなし・LLMはAPI呼び出しのため負荷は軽い） |
| ディスク | 書類量に依存。目安 10〜50GB（原本＋抽出JSON） |
| ネットワーク | **アウトバウンドHTTPSのみ**（宛先: ses.lykuro.ai と各社LLM API）。受信ポートの外部開放は不要 |
| データ保存 | ローカルディスク（フォルダ＋JSON または SQLite）。DBサーバー不要 |

管理画面（Web UI）を持たせる場合も、同一プロセスが社内LAN限定で1ポート（例: 8787）を
開くのみとし、インターネット側には公開しない。

### 6.2 LLM接続の形態

| 形態 | 必要なもの | 備考 |
|---|---|---|
| クラウドLLM（OpenAI互換） | APIキーのみ | 最も手軽。匿名化を通してから送る |
| 社内LLM（Ollama等） | 社内マシン（小型モデルはCPU可、GPU推奨） | データが社外に出ない。精度はモデル次第 |

### 6.3 配置パターン

1. **担当者PCに常駐**（1〜2名）: タスクスケジューラ起動。サーバー調達不要。PC停止中は止まる。
2. **社内常設マシン**（推奨・部署利用）: systemd / Windowsサービスで常駐。
   共有フォルダを監視対象にすれば部署全員の書類が自動で入る。
3. **各社契約のクラウドVM**: 最小構成VPSで可。書類がそのクラウドに乗る点は各社判断。

配布形態は「Node.js直接実行」に加え、Dockerイメージの提供も可能とする。

### 6.4 運用責任の分界

- OS更新・バックアップ・アクセス管理・ローカルデータの個人情報管理は**各社の責任**。
- 親サーバ側が保証するのはトークン/アカウント単位のアクセス制御と監査記録まで。
- プラットフォームの保存期限（2年）・本人削除請求の仕組みはローカル複製には及ばない。
  この旨をREADME・利用規約に明記する。

## 7. 決定済み事項（経緯）

- 2026-08-17: Windows取込ツール案（v0.1）中止、メール取込は検討対象外
- 2026-08-17: 呼称を「ローカルサーバ」に統一。公開リポジトリ `lykuroai/lykuro-connector` 作成済み（空）
- 2026-08-17: 機能方針=ローカル保存＋自前LLM解析（OCRなし）、商談開始時に親へ送信
- 2026-08-17: 親サーバ認証はPAT方式（§4.1。親サーバ側は未実装）
- 参考実装第1版（フォルダ監視→親サーバ送信のみ）は本体リポジトリ `examples/child-server/` に
  コミット済み（未公開。呼称改称・機能拡張は未反映）

## 8. 未確定論点

1. ローカル保存の形: フォルダ＋JSONで十分か、SQLite＋一覧・検索画面まで持つか
2. 商談開始時の送信操作: どの画面・操作で実行するか（Web画面／コマンド）
3. 送信先API: 直接登録（`POST /projects`/`engineers`）か書類取込（`/ingestions`）か
4. プロンプト・出力スキーマの同梱公開の可否
5. ローカルサーバ自体のログイン: MVPを認証なしで始めるか
6. 公開のタイミング・範囲・ライセンス
