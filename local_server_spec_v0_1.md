# ローカルサーバ（lykuro-connector）仕様書 v0.2 ドラフト

作成日: 2026-08-17 ／ 最終更新: 2026-08-18（v0.2: §9 追加仕様） ／ 状態: **検討中ドラフト**（確定仕様ではない）
※ファイル名はコード参照互換のため `v0_1` のまま。

本書は、SESマッチングプラットフォーム（ses.lykuro.ai、以下「親サーバ」）と連携する
**ローカルサーバ**の仕様を定める。プラットフォーム本体の正となる仕様は
`ses_matching_platform_complete_spec_v2_0.md` であり、本書はその追加仕様（連携ツール編）に位置づける。

---

## 1. 目的と位置づけ

- 各テナント企業が**自社環境内**で案件・人材書類を収集・構造化・保管し、
  **商談開始のタイミングで必要な1件だけを親サーバへ送信**するためのツール。
- データ主権は各社に残る。「全データをプラットフォームに預けるのは抵抗がある」という
  導入障壁を解消し、「自社の書類整理ツールとして使い始め、商談に出す分だけ登録する」
  段階的導入を可能にする。
- 公開リポジトリ: `https://github.com/lykuroai/lykuro-connector`（公開時期・ライセンスは未確定）
- 標準品としてのホスティングは行わない。**参考実装の提供まで**とし、構築・運用責任は各社にある。

## 2. 機能仕様

### 2.1 処理フロー

```
書類（フォルダ投入）
  → テキスト抽出（テキスト系形式のみ・OCRなし）
  → PII匿名化（ローカル正規表現処理。本体 pii.ts と同等ロジック）
  → 自前LLMで種別判定・構造化抽出
  → ローカル保存（原本 + 抽出JSON = 自社の案件・人材在庫）

商談開始時: 選択した案件・人材を親サーバへ送信 → 以後は親サーバの既存フロー
（登録 → 公開 → 商談申込み → 双方承認 → 契約 → 手数料）
```

### 2.2 動作モード

| モード | 動作 |
|---|---|
| ローカル保存（既定） | 親へ送らず、解析結果とともに自社内に保管 |
| 親サーバへ送信 | 商談開始時に、その1件を親サーバへ送信 |

- 送信は書類単位・明示操作で行う（自動一括送信はしない）。
- 送信後のデータの正は親サーバ側とする。ローカル側との同期機能は持たない
  （送信時点のスナップショットを登録する割り切り）。
- **v0.2 で変更**: 「親サーバへ送信」（書類のまま送信）は一時停止し、**公開送信**方式へ移行する（§9）。

### 2.3 対応ファイル形式

- テキスト系のみ: .txt / .csv / .md / Word (.docx) / Excel (.xlsx, .xls) / テキスト層のあるPDF
- **OCRは搭載しない**。画像・スキャン書類は親サーバへ直接送信すれば親側で解析される旨を案内する。

### 2.4 種別の扱い

- 案件 / 人材はフォルダ（または画面操作）で指定し、親サーバ送信時は `expectedKind` を付与する。
  親サーバ側の種別チェック（不一致は登録拒否）が最終防壁となる。
- 人材は1書類=1名。案件は親サーバ送信時に親側で複数件自動分割される。

## 3. 自前LLM解析

- 接続先は **OpenAI互換API** とし、各社が自社契約のAPIキーまたは社内LLM（Ollama等）を設定する。
  LLM費用・鍵管理は各社持ち。
- 種別判定・構造化抽出のプロンプトとJSON出力スキーマは参考実装に同梱する
  （※本体の抽出ロジックの一部公開になるため、公開前にkakuの判断を要する）。
- **外部LLMへ送る前に必ずローカル匿名化を通す**構成を標準とし、READMEで作法として明示する。
- ローカル抽出結果は各社の参考データであり、親サーバの正規化（スキル辞書・名寄せ・人手確認）
  とは一致しない場合がある。親へ送信した場合は親側の処理結果を正とする。

## 4. 親サーバ連携

### 4.1 認証 — PAT（APIトークン）方式【親サーバ側の追加開発】

- トークン形式: `ses_pat_` + ランダム32バイトhex。`Authorization: Bearer` で送信。
  DBにはSHA-256ハッシュのみ保存し、平文は発行時に一度だけ表示。
- **メンバー単位で発行**。メンバー退職・無効化、企業停止で自動失効。
- 権限スコープ（メンバー本人の権限との積集合で実効権限を決定）:
  - `ingest`（既定）= `ingestion.create`
  - `ingest-register` = 上記 + `engineer.create` / `project.create`
  - 商談申込みの承認・契約・請求・メンバー管理はスコープに含めない。
- 有効期限: 90日 / 1年（既定） / 無期限。期限30日前に発行者へ通知。
- コンソール「APIトークン」タブ: 発行（名前・スコープ・期限）／一覧（最終使用日時含む）／失効。
- 監査: `ApiTokenIssued` / `ApiTokenRevoked` を記録。PAT経由の操作は発行メンバーの操作として記録。
- PAT実装までの暫定運用: 取込専用の担当者アカウント（最小権限ロール）＋パスワードは
  環境変数または権限600の設定ファイルで保持。

### 4.2 使用API

| API | 用途 |
|---|---|
| `POST /api/v1/ingestions` | 書類のまま送信（親側でPII匿名化→LLM解析→人手確認） |
| `POST /api/v1/projects` / `POST /api/v1/engineers` | ローカル構造化済みデータの直接登録（親のLLM費用ゼロ。画面手入力と同格） |
| `GET /api/v1/ingestions/:id` | 取込状況の確認 |

- 商談開始時の連携は**公開送信方式に確定**（2026-08-18・§9.1。構造化データの直接登録）。書類取込（`/ingestions`）連携の扱いは §8 論点5。
- v0.2 で検索・提案用に `GET /projects` / `GET /engineers`（`scope=public`）・`POST /entries` を追加利用する（§9.3〜9.5）。

## 5. ローカルサーバ自体のログイン（社内利用者の管理）

| 段階 | 方式 |
|---|---|
| MVP | 認証なし。localhost または社内LAN限定で運用（外部非公開をREADMEで必須要件化） |
| 拡張 | 親アカウントで認証委譲（ログイン時に親の `/auth/login` で検証。ID二重管理なし、親側の無効化が即反映） |

## 6. インフラ環境（追加仕様）

### 6.1 動作要件（最小）

| 項目 | 要件 |
|---|---|
| 実行環境 | Node.js 20 以上（Windows / Linux / macOS） |
| CPU / メモリ | 1コア / 1〜2GB（OCRなし・LLMはAPI呼び出しのため負荷は軽い） |
| ディスク | 書類量に依存。目安 10〜50GB（原本＋抽出JSON） |
| ネットワーク | **アウトバウンドHTTPSのみ**（宛先: ses.lykuro.ai と各社LLM API）。受信ポートの外部開放は不要 |
| データ保存 | ローカルディスク（フォルダ＋JSON または SQLite）。DBサーバー不要 |

管理画面（Web UI）を持たせる場合も、同一プロセスが社内LAN限定で1ポート（例: 8787）を
開くのみとし、インターネット側には公開しない。

### 6.2 LLM接続の形態

| 形態 | 必要なもの | 備考 |
|---|---|---|
| クラウドLLM（OpenAI互換） | APIキーのみ | 最も手軽。匿名化を通してから送る |
| 社内LLM（Ollama等） | 社内マシン（小型モデルはCPU可、GPU推奨） | データが社外に出ない。精度はモデル次第 |

### 6.3 配置パターン

1. **担当者PCに常駐**（1〜2名）: タスクスケジューラ起動。サーバー調達不要。PC停止中は止まる。
2. **社内常設マシン**（推奨・部署利用）: systemd / Windowsサービスで常駐。
   共有フォルダを監視対象にすれば部署全員の書類が自動で入る。
3. **各社契約のクラウドVM**: 最小構成VPSで可。書類がそのクラウドに乗る点は各社判断。

配布形態は「Node.js直接実行」に加え、Dockerイメージの提供も可能とする。

### 6.4 運用責任の分界

- OS更新・バックアップ・アクセス管理・ローカルデータの個人情報管理は**各社の責任**。
- 親サーバ側が保証するのはトークン/アカウント単位のアクセス制御と監査記録まで。
- プラットフォームの保存期限（2年）・本人削除請求の仕組みはローカル複製には及ばない。
  この旨をREADME・利用規約に明記する。

## 7. 決定済み事項（経緯）

- 2026-08-17: Windows取込ツール案（v0.1）中止、メール取込は検討対象外
- 2026-08-17: 呼称を「ローカルサーバ」に統一。公開リポジトリ `lykuroai/lykuro-connector` 作成済み（空）
- 2026-08-17: 機能方針=ローカル保存＋自前LLM解析（OCRなし）、商談開始時に親へ送信
- 2026-08-17: 親サーバ認証はPAT方式（§4.1。親サーバ側は未実装）
- 参考実装第1版（フォルダ監視→親サーバ送信のみ）は本体リポジトリ `examples/child-server/` に
  コミット済み（未公開。呼称改称・機能拡張は未反映）
- 2026-08-17: PAT方式を親サーバ側に実装、参考実装を `examples/local-server/` として
  `lykuroai/lykuro-connector` へ公開（MITライセンス・インストールバッチ同梱）
- 2026-08-18: 管理画面にファイルアップロード取込・テキスト貼り付け取込を追加
- 2026-08-18: 送信仕様の変更（§9）に伴い「親サーバへ送信」（書類のまま送信）を一時停止
- 2026-08-18: §8 旧論点3を確定 — 親サーバ連携は**公開送信方式**（§9.1）。
  検索・人材提案・案件紹介機能を追加仕様として決定（§9.3・§9.4）
- 2026-08-18: 公開送信は登録と同時に公開まで実行。人材の本人同意は手動入力から
  **登録時の自動同意（同意日=登録日）**へ変更（親・ローカル共通、§9.2）

## 8. 未確定論点

1. ローカル保存の形: フォルダ＋JSONで十分か、SQLite＋一覧・検索画面まで持つか
2. プロンプト・出力スキーマの同梱公開の可否
3. ローカルサーバ自体のログイン: MVPを認証なしで始めるか
4. §9 のPATスコープ名・粒度（実装時に確定）
5. 一時停止中の書類送信機能（`/ingestions` 連携）の扱い: 公開送信へ完全置換して廃止するか、
   画像・スキャン書類向けの補助経路として残すか

## 9. 追加仕様（v0.2・2026-08-18）: 公開送信・検索・人材提案／案件紹介

### 9.1 方針の変更

- 商談開始時の親サーバ連携を「書類のまま送信（`POST /ingestions`）」から
  **「公開送信」**（ローカル構造化データを `POST /projects` / `POST /engineers` で直接登録）へ変更する
  （§8 論点3を確定）。これに伴い書類送信機能は一時停止済み（参考実装 `SEND_TO_PARENT_ENABLED = false`）。
- 用語: 機能名は**「人材提案」「案件紹介」**とする。「エントリー」「スカウト」の表現は用いない。

### 9.2 公開送信（親サーバへの直接登録）

- ローカル在庫の案件・人材（抽出JSON）を、親サーバの登録APIへ構造化データとして登録する。
  親の画面手入力と同格の扱いで、親側のLLM解析・取込確認は通らない。
- **登録前にローカル画面で内容を確認・修正**できること（確認責任はローカル側に移る）。
- 親の必須項目のうちローカル抽出に無いものは登録画面で補完入力する。
  特に人材の**氏名**（ローカルではPII匿名化で保持しない）・所属種別は登録時に入力が必要。
- PATスコープは既存の `ingest-register`（`engineer.create` / `project.create`）を使用。
- 登録後のデータの正は親サーバ側（同期なし・スナップショット登録の割り切りは従来どおり）。
  ローカル側は登録済みマークと親側IDを記録する。
- **公開送信は登録と同時に公開（企業間公開）まで行う**（名称のとおり「公開のための送信」）。
- 人材の本人同意は**手動入力から自動同意へ変更**（親・ローカル共通、2026-08-18）:
  登録時に同意記録を自動作成する（**同意日 = 登録日**）。
  登録企業が事前に本人から同意を得ていることが前提であり、その旨を利用規約・画面注記で明示する。

### 9.3 親サーバの案件・人材検索

- ローカル画面から、親サーバの**企業間公開の検索**（他社の公開案件・公開人材）を行う。
  検索条件・表示は親コンソールと同じとし、他社分は Level 1（匿名情報）のみ。
- **検索結果には自社の公開分も含める**（2026-08-18 決定。「自社」バッジで区別し、
  自社の行には提案・紹介ボタンを出さない）。親コンソールと同様に
  `scope=public`（他社公開分）と `scope=own`（自社分・公開済みのみ）を併取得して統合する。
- 親サーバの既存API `GET /projects` / `GET /engineers`（`scope`・キーワード・ページング対応）を利用する（親サーバの変更は不要）。
- 検索対象は進行中のもののみ（案件=募集中／人材=紹介中。2026-08-18 決定）。
- **マッチ絞り込み**（2026-08-18 追加）: 検索時にローカル在庫の人材／案件をマッチ対象として指定でき、
  本体 §19 の簡易版（接尾辞正規化のみ・ローカルに無い項目は判定除外）をローカルサーバ内で計算して
  適合分のみスコア順に表示する。公開人材の単価は帯の下限で判定。親サーバへの追加送信は発生しない。

### 9.4 人材提案・案件紹介

- 検索で見つけた他社の公開案件へ、親サーバ登録済みの自社人材を**提案**する。
- 検索で見つけた他社の公開人材へ、親サーバ登録済みの自社案件を**紹介**する。
- ローカルから行えるのは提案・紹介の**作成まで**とし、以後の商談フロー
  （承認・面談・成約）は親サーバの既存画面で進める。
- 前提: 対象の自社人材・自社案件が公開送信済みであること（未送信なら §9.2 を先に行う）。
- 親サーバの既存API `POST /entries` を利用する（重複提案のブロック等は親側の既存ルールが適用される）。

### 9.5 親サーバ側の追加開発【仕様の明記のみ・実装は別途指示】

親サーバは現行ルールにより修正しない。実装は明示指示を受けてから行う。必要な追加開発:

- **PATスコープの拡張**（§4.1 の2スコープに追加。名称・粒度は実装時に確定）:
  - 検索: `project.read` / `engineer.read.masked`（scope=public の閲覧）
  - 公開送信の公開実行: `project.publish` / `engineer.publish`
  - 提案・紹介: `entry.submit` / `entry.read`
  - 引き続き、承認・契約・請求・メンバー管理はスコープに含めない（それらは親画面でのみ実行）。
- **人材の本人同意の変更（手動入力 → 自動同意）**: 人材登録時に同意記録を自動作成する
  （同意日 = 登録日）。親コンソールの手動同意入力画面の扱い（廃止か併存か）は実装時に確定。
- 検索・提案・公開APIのPAT経由呼び出しの動作確認（既存APIのため新規エンドポイントは不要の見込み）。

### 9.6 ローカル画面の構成（v0.2 時点の想定）

| 画面 | 機能 |
|---|---|
| 自社在庫（既存） | 取込（ファイル/貼り付け/フォルダ）・一覧・削除・**公開送信**（§9.2、旧「親サーバへ送信」の置き換え） |
| 親サーバ検索 | 公開案件・公開人材の検索（§9.3）と、結果からの**人材提案・案件紹介**（§9.4） |
